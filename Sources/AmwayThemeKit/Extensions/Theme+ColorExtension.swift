//
//  Theme + ColorExtension.swift
//
//  Copyright (c) 2021 Amway. All rights reserved.
//

import UIKit

public extension Theme {
    var amwayBlack: ColorType {
        let uiColor = UIColor.amwayBlack(theme: self)
        return ColorType(uiColor: uiColor)
    }

    var amwayWhite: ColorType {
        let uiColor = UIColor.amwayWhite(theme: self)
        return ColorType(uiColor: uiColor)
    }

    var white: ColorType {
        let uiColor = UIColor.white(theme: self)
        return ColorType(uiColor: uiColor)
    }

    var currentDayBackground: ColorType {
        let uiColor = UIColor.currentDayBackground(theme: self)
        return ColorType(uiColor: uiColor)
    }

    var create: ColorType {
        let uiColor = UIColor.create(theme: self)
        return ColorType(uiColor: uiColor)
    }

    var black: ColorType {
        let uiColor = UIColor.black(theme: self)
        return ColorType(uiColor: uiColor)
    }

    var darkPurple: ColorType {
        let uiColor = UIColor.darkPurple(theme: self)
        return ColorType(uiColor: uiColor)
    }

    var dim: ColorType {
        let uiColor = UIColor.dim(theme: self)
        return ColorType(uiColor: uiColor)
    }

    var lightPurple: ColorType {
        let uiColor = UIColor.lightPurple(theme: self)
        return ColorType(uiColor: uiColor)
    }

    var lightGray: ColorType {
        let uiColor = UIColor.lightGray(theme: self)
        return ColorType(uiColor: uiColor)
    }

    var lightBlue: ColorType {
        let uiColor = UIColor.lightBlue(theme: self)
        return ColorType(uiColor: uiColor)
    }

    var createDarkGreen: ColorType {
        let uiColor = UIColor.createDarkGreen(theme: self)
        return ColorType(uiColor: uiColor)
    }

    var gray1: ColorType {
        let uiColor = UIColor.gray1(theme: self)
        return ColorType(uiColor: uiColor)
    }

    var gray2: ColorType {
        let uiColor = UIColor.gray2(theme: self)
        return ColorType(uiColor: uiColor)
    }

    var gray4: ColorType {
        let uiColor = UIColor.gray4(theme: self)
        return ColorType(uiColor: uiColor)
    }

    var gray5: ColorType {
        let uiColor = UIColor.gray5(theme: self)
        return ColorType(uiColor: uiColor)
    }

    var gray6: ColorType {
        let uiColor = UIColor.gray6(theme: self)
        return ColorType(uiColor: uiColor)
    }

    var loaderBackgroundColor: ColorType {
        let uiColor = UIColor.loaderBackgroundColor(theme: self)
        return ColorType(uiColor: uiColor)
    }

    var backgroundGray: ColorType {
        let uiColor = UIColor.backgroundGray(theme: self)
        return ColorType(uiColor: uiColor)
    }

    var alertOrange: ColorType {
        let uiColor = UIColor.alertOrange(theme: self)
        return ColorType(uiColor: uiColor)
    }

    var primaryPurpleGradient: ColorType {
        let uiColor = UIColor.primaryPurpleGradient(theme: self)
        return ColorType(uiColor: uiColor)
    }

    var secondaryPurpleGradient: ColorType {
        let uiColor = UIColor.secondaryPurpleGradient(theme: self)
        return ColorType(uiColor: uiColor)
    }

    var modalBackground: ColorType {
        let uiColor = UIColor.modalBackground(theme: self)
        return ColorType(uiColor: uiColor)
    }

    var success1: ColorType {
        let uiColor = UIColor.success1(theme: self)
        return ColorType(uiColor: uiColor)
    }

    var success2: ColorType {
        let uiColor = UIColor.success2(theme: self)
        return ColorType(uiColor: uiColor)
    }

    var success3: ColorType {
        let uiColor = UIColor.success3(theme: self)
        return ColorType(uiColor: uiColor)
    }

    var error18: ColorType {
        let uiColor = UIColor.error18(theme: self)
        return ColorType(uiColor: uiColor)
    }

    var error19: ColorType {
        let uiColor = UIColor.error19(theme: self)
        return ColorType(uiColor: uiColor)
    }

    var error20: ColorType {
        let uiColor = UIColor.error20(theme: self)
        return ColorType(uiColor: uiColor)
    }

    var tagBlue: ColorType {
        let uiColor = UIColor.tagBlue(theme: self)
        return ColorType(uiColor: uiColor)
    }

    var tagLightBlue: ColorType {
        let uiColor = UIColor.tagLightBlue(theme: self)
        return ColorType(uiColor: uiColor)
    }

    var darkOrange: ColorType {
        let uiColor = UIColor.darkOrange(theme: self)
        return ColorType(uiColor: uiColor)
    }

    var lightOrange: ColorType {
        let uiColor = UIColor.lightOrange(theme: self)
        return ColorType(uiColor: uiColor)
    }

    var lightRed: ColorType {
        let uiColor = UIColor.lightRed(theme: self)
        return ColorType(uiColor: uiColor)
    }

    var darkRed: ColorType {
        let uiColor = UIColor.darkRed(theme: self)
        return ColorType(uiColor: uiColor)
    }

    var grayDisabled: ColorType {
        let uiColor = UIColor.grayDisabled(theme: self)
        return ColorType(uiColor: uiColor)
    }
}
